@2440启动文件的作用 硬件初始化和软件初始化
@硬件初始化 包括 1看门狗初始化  2SDRAN初始化  3系统时钟初始化
@软件初始化 包括 1设置栈，函数返回地址  2跳转到main函数执行 3执行完main函数后的清理工作
@因为这里SDRAM没有使用则 没有设置，系统始终采用系统复位后默认的时钟 所以也无须设置
.text
.global _start
_start:
	ldr r0, =0x53000000 @看门狗定时器的地址  
	mov r1, #0  
	str r1, [r0]
	
	mov sp, #4096	@因为2440的内部sram是4k 栈顶也就是4096
	
	bl 	main      @跳转到mian函数 并保存返回地址到lr寄存器
loop:
	b loop


	
